apiVersion: v1
kind: Service
metadata:
  name: {{ include "docker-ics-view.fullname" . }}
  labels:
    {{- include "docker-ics-view.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "docker-ics-view.selectorLabels" . | nindent 4 }}
